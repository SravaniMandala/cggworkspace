 export const Greet = (name,company,children) => {
  // console.log(props);
  // const {name,company,children}=props;
  return (
    <div>
      <h1>Greeting from {name} and {company}
      {children}
      </h1>
    </div>
  );
};

// export default Greet
